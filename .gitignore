# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.production

# Supabase auto-generated
src/integrations/
# supabase/config.toml — committed to share disabled-migration setup (§117 RLS testing)

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
supabase/.temp/

# Worktrees (parallel agent sessions, not part of the trunk)
.worktrees/
.claude/worktrees/

# Personal docs / spreadsheets dragged into /docs (not project artifacts).
# *.xlsx is global on purpose: any .xlsx in /docs is a draft / personal
# scratch and shouldn't enter the repo. Move to /supabase/seeds or rename
# to a non-xlsx format if you genuinely need to track it.
docs/*.xlsx
docs/*.xls
