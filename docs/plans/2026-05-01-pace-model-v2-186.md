# §186 — Pace Model v2 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task.

**Goal:** Replace the linear pace model (V0..MAX as % of target time) with the non-linear model from `regles_calcul_allures_natation.docx`: target-event-specific R_base curves + per-stroke A_nage adjustment + speed coefficients k by event family + V4 zone + segmented 4N.

**Architecture:** Three rings. Innermost = pure non-linear math (`paceCalculatorV2.ts` + `paceData.ts`). Middle = data access (refonte of `coach_pace_zones`, new tables `pace_ratios_base`, `pace_stroke_adjustments`, `coach_stroke_adjustments`). Outer = UI (rebuild `PaceMatrix`, `PaceZonesSettings` with event-family tabs, new `PaceStrokeAdjustments` drawer, dedicated `Pace4NSegmentMatrix`).

**Tech Stack:** React 19, TypeScript, Vite, Tailwind, Radix/Shadcn, React Query 5, Wouter, node:test (NOT Vitest — see `src/lib/api/__tests__/assignments.test.ts` for the pattern), Supabase + RLS, MCP plugin for migrations.

**Reference design:** `docs/plans/2026-05-01-pace-model-v2-186-design.md` (committed `6ffebd43c`).

**Source métier authoritative:** `docs/regles_calcul_allures_natation.docx`. Les tables et exemples §12 du doc sont la vérité — chaque cellule des exemples §12.1 / §12.2 / §12.3 devient un test unitaire.

**Key conventions (read before starting):**
- Migrations via MCP `mcp__plugin_supabase_supabase__apply_migration` only. Project ID `fscnobivsgornxdwqwlk`. Number = `00151_*.sql`.
- RLS via `(SELECT auth.uid()) = coach_id`, mirror existing `coach_pace_zones` policies pattern.
- API modules in `src/lib/api/<name>.ts`, re-exported via `src/lib/api/index.ts` and aggregated façade `src/lib/api.ts`.
- **Tests use `node:test` + `mock.module`**, NOT Vitest's `vi.*`. Calque `src/lib/api/__tests__/assignments.test.ts`. (Erreur évitée explicitement, voir §184 fix `61b4d119c`.)
- Run tests : `npm test -- --run` (X pass / 1 fail attendu — `transformers.test.ts` est pré-existant).
- RLS tests : `npm run test:rls` (Docker requis — vérifier `docker ps` 1× max par session, demander à l'utilisateur si Docker down).
- Coach sections registered in `src/pages/Coach.tsx`, hash routing `/#/coach?section=<name>`. La section `pace-calculator` existe déjà (§184).
- Strokes API : interne v2 utilise `crawl/dos/brasse/papillon/4N`. Mapping NL ↔ crawl géré dans `paceCalculatorV2.ts`.
- After implementation : update `docs/implementation-log.md`, `docs/ROADMAP.md`, `docs/FEATURES_STATUS.md`, `CLAUDE.md`, `docs/claude/files-map.md` for new ≥150 LOC files.

**Worktree:** Recommend `chantier/186-pace-model-v2` worktree via `superpowers:using-git-worktrees`.

**TDD order:** Pure data + math first (no DB), then migration + RLS, then API + hooks, then UI rebuild, then RLS test batch, then docs.

**Token economy:** Cf. CLAUDE.md § Économie de tokens. `docker ps` 1× max, ne pas relire `assignments.test.ts` plus de 2× (calquer dans la mémoire), pas de lecture de `docs/rls-testing.md` sauf debug.

---

## Phase 1 — Pure data tables

### Task 1: `paceData.ts` — RATIOS_BASE table

**Files:**
- Create: `src/lib/paceData.ts`
- Test: `src/__tests__/paceData.test.ts`

**Step 1: Failing test**

```ts
// src/__tests__/paceData.test.ts
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { RATIOS_BASE, getRatioAnchors } from "../lib/paceData";

describe("RATIOS_BASE", () => {
  it("contains all 6 event families", () => {
    assert.deepEqual(
      Object.keys(RATIOS_BASE).map(Number).sort((a, b) => a - b),
      [50, 100, 200, 400, 800, 1500],
    );
  });

  it("each curve ends at ratio 1.000 for the target distance", () => {
    for (const D of [50, 100, 200, 400, 800, 1500]) {
      const anchors = RATIOS_BASE[D];
      const last = anchors[anchors.length - 1];
      assert.equal(last.d, D);
      assert.equal(last.ratio, 1.0);
    }
  });

  it("each curve is strictly increasing in d and ratio", () => {
    for (const D of [50, 100, 200, 400, 800, 1500]) {
      const anchors = RATIOS_BASE[D];
      for (let i = 1; i < anchors.length; i++) {
        assert.ok(anchors[i].d > anchors[i - 1].d);
        assert.ok(anchors[i].ratio > anchors[i - 1].ratio);
      }
    }
  });

  it("getRatioAnchors(50) returns 8 points matching doc §5.1", () => {
    const a = getRatioAnchors(50);
    assert.deepEqual(a, [
      { d: 15, ratio: 0.241 },
      { d: 20, ratio: 0.346 },
      { d: 25, ratio: 0.451 },
      { d: 30, ratio: 0.561 },
      { d: 35, ratio: 0.671 },
      { d: 40, ratio: 0.780 },
      { d: 45, ratio: 0.890 },
      { d: 50, ratio: 1.000 },
    ]);
  });
});
```

**Step 2: Run — expect FAIL**
```bash
npm test -- src/__tests__/paceData.test.ts
```

**Step 3: Implement**

```ts
// src/lib/paceData.ts
export type RatioAnchor = { d: number; ratio: number };

export const RATIOS_BASE: Record<number, RatioAnchor[]> = {
  50: [
    { d: 15, ratio: 0.241 }, { d: 20, ratio: 0.346 }, { d: 25, ratio: 0.451 },
    { d: 30, ratio: 0.561 }, { d: 35, ratio: 0.671 }, { d: 40, ratio: 0.780 },
    { d: 45, ratio: 0.890 }, { d: 50, ratio: 1.000 },
  ],
  100: [
    { d: 15, ratio: 0.114 }, { d: 25, ratio: 0.214 }, { d: 35, ratio: 0.316 },
    { d: 50, ratio: 0.470 }, { d: 75, ratio: 0.735 }, { d: 100, ratio: 1.000 },
  ],
  200: [
    { d: 25, ratio: 0.115 }, { d: 50, ratio: 0.235 }, { d: 75, ratio: 0.359 },
    { d: 100, ratio: 0.485 }, { d: 150, ratio: 0.740 }, { d: 200, ratio: 1.000 },
  ],
  400: [
    { d: 50, ratio: 0.120 }, { d: 100, ratio: 0.245 }, { d: 150, ratio: 0.370 },
    { d: 200, ratio: 0.496 }, { d: 300, ratio: 0.747 }, { d: 400, ratio: 1.000 },
  ],
  800: [
    { d: 50, ratio: 0.061 }, { d: 100, ratio: 0.124 }, { d: 200, ratio: 0.249 },
    { d: 400, ratio: 0.500 }, { d: 600, ratio: 0.751 }, { d: 800, ratio: 1.000 },
  ],
  1500: [
    { d: 50, ratio: 0.032 }, { d: 100, ratio: 0.066 }, { d: 200, ratio: 0.133 },
    { d: 400, ratio: 0.266 }, { d: 800, ratio: 0.534 }, { d: 1000, ratio: 0.668 },
    { d: 1500, ratio: 1.000 },
  ],
};

export function getRatioAnchors(D: number): RatioAnchor[] {
  const anchors = RATIOS_BASE[D];
  if (!anchors) throw new Error(`Unknown event distance: ${D}`);
  return anchors;
}
```

**Step 4: Run — expect PASS**

**Step 5: Commit**
```bash
git add src/lib/paceData.ts src/__tests__/paceData.test.ts
git commit -m "feat(pace-v2): RATIOS_BASE table from doc §5"
```

---

### Task 2: ZONE_COEFFICIENTS table

**Files:**
- Modify: `src/lib/paceData.ts`
- Modify: `src/__tests__/paceData.test.ts`

**Step 1: Failing tests**

```ts
import { ZONE_COEFFICIENTS, type EventFamily, type Zone } from "../lib/paceData";

describe("ZONE_COEFFICIENTS", () => {
  it("matches doc §4 for all 5 event families", () => {
    assert.deepEqual(ZONE_COEFFICIENTS["50m"], { V0: 0.70, V1: 0.78, V2: 0.86, V3: 0.94, V4: 0.98, MAX: 1.00 });
    assert.deepEqual(ZONE_COEFFICIENTS["100m"], { V0: 0.72, V1: 0.80, V2: 0.88, V3: 0.95, V4: 0.98, MAX: 1.00 });
    assert.deepEqual(ZONE_COEFFICIENTS["200m"], { V0: 0.74, V1: 0.82, V2: 0.90, V3: 0.96, V4: 0.985, MAX: 1.00 });
    assert.deepEqual(ZONE_COEFFICIENTS["400m"], { V0: 0.76, V1: 0.84, V2: 0.91, V3: 0.96, V4: null, MAX: 1.00 });
    assert.deepEqual(ZONE_COEFFICIENTS["800m_1500m"], { V0: 0.78, V1: 0.86, V2: 0.92, V3: 0.97, V4: null, MAX: 1.00 });
  });

  it("V4 is null on 400m and 800m_1500m families (V4 disabled by default)", () => {
    assert.equal(ZONE_COEFFICIENTS["400m"].V4, null);
    assert.equal(ZONE_COEFFICIENTS["800m_1500m"].V4, null);
  });

  it("k_speed is monotonic V0 < V1 < V2 < V3 < (V4) < MAX in each family", () => {
    const families: EventFamily[] = ["50m", "100m", "200m", "400m", "800m_1500m"];
    for (const f of families) {
      const c = ZONE_COEFFICIENTS[f];
      assert.ok(c.V0 < c.V1);
      assert.ok(c.V1 < c.V2);
      assert.ok(c.V2 < c.V3);
      if (c.V4 !== null) assert.ok(c.V3 < c.V4 && c.V4 < c.MAX);
      else assert.ok(c.V3 < c.MAX);
    }
  });
});
```

**Step 2: Run — FAIL**

**Step 3: Implement**

```ts
// Append to src/lib/paceData.ts
export type EventFamily = "50m" | "100m" | "200m" | "400m" | "800m_1500m";
export type Zone = "V0" | "V1" | "V2" | "V3" | "V4" | "MAX";

export interface FamilyCoefficients {
  V0: number;
  V1: number;
  V2: number;
  V3: number;
  V4: number | null;
  MAX: number;
}

export const ZONE_COEFFICIENTS: Record<EventFamily, FamilyCoefficients> = {
  "50m":          { V0: 0.70, V1: 0.78, V2: 0.86, V3: 0.94, V4: 0.98,  MAX: 1.00 },
  "100m":         { V0: 0.72, V1: 0.80, V2: 0.88, V3: 0.95, V4: 0.98,  MAX: 1.00 },
  "200m":         { V0: 0.74, V1: 0.82, V2: 0.90, V3: 0.96, V4: 0.985, MAX: 1.00 },
  "400m":         { V0: 0.76, V1: 0.84, V2: 0.91, V3: 0.96, V4: null,  MAX: 1.00 },
  "800m_1500m":   { V0: 0.78, V1: 0.86, V2: 0.92, V3: 0.97, V4: null,  MAX: 1.00 },
};
```

**Step 4: PASS — Step 5: Commit**
```bash
git commit -m "feat(pace-v2): ZONE_COEFFICIENTS by event family + V4 nullability"
```

---

### Task 3: STROKE_ADJUSTMENTS_DEFAULT table (mS médianes)

**Files:**
- Modify: `src/lib/paceData.ts`
- Modify: `src/__tests__/paceData.test.ts`

**Step 1: Tests** (median of doc §7 ranges)

```ts
import { STROKE_ADJUSTMENTS_DEFAULT, type StrokeV2 } from "../lib/paceData";

describe("STROKE_ADJUSTMENTS_DEFAULT", () => {
  it("crawl is 0 across all families (reference)", () => {
    for (const f of ["50m", "100m", "200m", "400m", "800m_1500m"] as EventFamily[]) {
      assert.equal(STROKE_ADJUSTMENTS_DEFAULT.crawl[f], 0);
    }
  });

  it("dos uses median of doc ranges (50m: 0.06, 100m: 0.045, 200m: 0.02, ≥400m: 0.01)", () => {
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.dos["50m"], 0.06);
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.dos["100m"], 0.045);
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.dos["200m"], 0.02);
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.dos["400m"], 0.01);
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.dos["800m_1500m"], 0.01);
  });

  it("brasse: 50m=0.04, 100m=0.035, 200m=0.025, ≥400m=0.01", () => {
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.brasse["50m"], 0.04);
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.brasse["100m"], 0.035);
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.brasse["200m"], 0.025);
  });

  it("papillon stays low: 50m=0, 100m=0, 200m=0.01, ≥400m=0.01", () => {
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.papillon["50m"], 0);
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.papillon["100m"], 0);
    assert.equal(STROKE_ADJUSTMENTS_DEFAULT.papillon["200m"], 0.01);
  });
});
```

**Step 3: Implement**

```ts
// Append to src/lib/paceData.ts
export type StrokeV2 = "crawl" | "dos" | "brasse" | "papillon" | "4N";

// "4N" not in adjustments — handled separately via segmentation.
type SingleStroke = "crawl" | "dos" | "brasse" | "papillon";

export const STROKE_ADJUSTMENTS_DEFAULT: Record<SingleStroke, Record<EventFamily, number>> = {
  crawl:    { "50m": 0.00,  "100m": 0.00,  "200m": 0.00,  "400m": 0.00, "800m_1500m": 0.00 },
  papillon: { "50m": 0.00,  "100m": 0.00,  "200m": 0.01,  "400m": 0.01, "800m_1500m": 0.01 },
  dos:      { "50m": 0.06,  "100m": 0.045, "200m": 0.02,  "400m": 0.01, "800m_1500m": 0.01 },
  brasse:   { "50m": 0.04,  "100m": 0.035, "200m": 0.025, "400m": 0.01, "800m_1500m": 0.01 },
};
```

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): STROKE_ADJUSTMENTS_DEFAULT (médianes du doc §7)"
```

---

### Task 4: SEGMENTS_4N table (poids par segment)

**Files:**
- Modify: `src/lib/paceData.ts`
- Modify: `src/__tests__/paceData.test.ts`

**Step 1: Tests**

```ts
import { SEGMENTS_4N } from "../lib/paceData";

describe("SEGMENTS_4N", () => {
  it("200 4N segments sum to 1.000", () => {
    const sum = SEGMENTS_4N["200"].reduce((a, s) => a + s.weight, 0);
    assert.ok(Math.abs(sum - 1.0) < 1e-6, `sum=${sum}`);
  });
  it("400 4N segments sum to 1.000", () => {
    const sum = SEGMENTS_4N["400"].reduce((a, s) => a + s.weight, 0);
    assert.ok(Math.abs(sum - 1.0) < 1e-6, `sum=${sum}`);
  });
  it("200 4N : papillon=0.218, dos=0.250, brasse=0.290, crawl=0.242", () => {
    assert.deepEqual(SEGMENTS_4N["200"], [
      { stroke: "papillon", segment_distance: 50, weight: 0.218 },
      { stroke: "dos",      segment_distance: 50, weight: 0.250 },
      { stroke: "brasse",   segment_distance: 50, weight: 0.290 },
      { stroke: "crawl",    segment_distance: 50, weight: 0.242 },
    ]);
  });
  it("400 4N : papillon=0.229, dos=0.255, brasse=0.280, crawl=0.236", () => {
    assert.deepEqual(SEGMENTS_4N["400"], [
      { stroke: "papillon", segment_distance: 100, weight: 0.229 },
      { stroke: "dos",      segment_distance: 100, weight: 0.255 },
      { stroke: "brasse",   segment_distance: 100, weight: 0.280 },
      { stroke: "crawl",    segment_distance: 100, weight: 0.236 },
    ]);
  });
});
```

**Step 3: Implement**

```ts
export interface FourNSegment {
  stroke: "papillon" | "dos" | "brasse" | "crawl";
  segment_distance: number;
  weight: number;
}

export const SEGMENTS_4N: Record<"200" | "400", FourNSegment[]> = {
  "200": [
    { stroke: "papillon", segment_distance: 50, weight: 0.218 },
    { stroke: "dos",      segment_distance: 50, weight: 0.250 },
    { stroke: "brasse",   segment_distance: 50, weight: 0.290 },
    { stroke: "crawl",    segment_distance: 50, weight: 0.242 },
  ],
  "400": [
    { stroke: "papillon", segment_distance: 100, weight: 0.229 },
    { stroke: "dos",      segment_distance: 100, weight: 0.255 },
    { stroke: "brasse",   segment_distance: 100, weight: 0.280 },
    { stroke: "crawl",    segment_distance: 100, weight: 0.236 },
  ],
};
```

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): SEGMENTS_4N — poids par segment (doc §9)"
```

---

## Phase 2 — Pure math (paceCalculatorV2)

### Task 5: `eventFamily()` + `normalizeStroke()`

**Files:**
- Create: `src/lib/paceCalculatorV2.ts`
- Test: `src/__tests__/paceCalculatorV2.test.ts`

**Step 1: Tests**

```ts
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { eventFamily, normalizeStroke } from "../lib/paceCalculatorV2";

describe("eventFamily", () => {
  it.skip; // node:test has no it.skip alias for that name; use simple it() per case
  for (const [D, expected] of [[50, "50m"], [100, "100m"], [200, "200m"], [400, "400m"], [800, "800m_1500m"], [1500, "800m_1500m"]] as const) {
    it(`maps D=${D} to ${expected}`, () => {
      assert.equal(eventFamily(D), expected);
    });
  }
  it("throws on unsupported distance", () => {
    assert.throws(() => eventFamily(150), /Unsupported event distance/);
  });
});

describe("normalizeStroke", () => {
  it("maps NL to crawl", () => assert.equal(normalizeStroke("NL"), "crawl"));
  it("maps Pap to papillon", () => assert.equal(normalizeStroke("Pap"), "papillon"));
  it("maps Dos to dos (lowercased)", () => assert.equal(normalizeStroke("Dos"), "dos"));
  it("maps Brasse to brasse", () => assert.equal(normalizeStroke("Brasse"), "brasse"));
  it("passes 4N through", () => assert.equal(normalizeStroke("4N"), "4N"));
  it("passes already-normalized values", () => assert.equal(normalizeStroke("crawl"), "crawl"));
  it("throws on unknown stroke", () => assert.throws(() => normalizeStroke("NAC")));
});
```

**Step 3: Implement**

```ts
// src/lib/paceCalculatorV2.ts
import type { EventFamily, StrokeV2 } from "./paceData";

export type { EventFamily, StrokeV2, Zone } from "./paceData";

const FAMILY_BY_DISTANCE: Record<number, EventFamily> = {
  50: "50m",
  100: "100m",
  200: "200m",
  400: "400m",
  800: "800m_1500m",
  1500: "800m_1500m",
};

export function eventFamily(D: number): EventFamily {
  const f = FAMILY_BY_DISTANCE[D];
  if (!f) throw new Error(`Unsupported event distance: ${D}`);
  return f;
}

const STROKE_ALIASES: Record<string, StrokeV2> = {
  NL: "crawl", crawl: "crawl",
  Pap: "papillon", papillon: "papillon",
  Dos: "dos", dos: "dos",
  Brasse: "brasse", brasse: "brasse",
  "4N": "4N",
};

export function normalizeStroke(s: string): StrokeV2 {
  const v = STROKE_ALIASES[s];
  if (!v) throw new Error(`Unknown stroke: ${s}`);
  return v;
}
```

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): eventFamily + normalizeStroke helpers"
```

---

### Task 6: `getRatio()` with log-linear interpolation

**Files:**
- Modify: `src/lib/paceCalculatorV2.ts`
- Modify: `src/__tests__/paceCalculatorV2.test.ts`

**Step 1: Tests (doc §6 + sanity)**

```ts
import { getRatio } from "../lib/paceCalculatorV2";

describe("getRatio (with log-linear interpolation)", () => {
  it("returns exact value for anchor distances", () => {
    assert.equal(getRatio(100, 50), 0.470);
    assert.equal(getRatio(100, 75), 0.735);
    assert.equal(getRatio(50, 25), 0.451);
  });

  it("interpolates 65m on 100m curve via log-linear", () => {
    // R = R1 * (R2/R1)^((d-d1)/(d2-d1))
    // R1=0.470 (50m), R2=0.735 (75m), d=65 → R = 0.470 × (0.735/0.470)^(15/25) = 0.470 × 1.5638^0.6 ≈ 0.6191
    const r = getRatio(100, 65);
    assert.ok(Math.abs(r - 0.6191) < 0.0005, `got ${r}`);
  });

  it("returns 1.000 at the target distance", () => {
    assert.equal(getRatio(50, 50), 1.000);
    assert.equal(getRatio(1500, 1500), 1.000);
  });

  it("throws on extrapolation below smallest anchor", () => {
    assert.throws(() => getRatio(50, 10), /below smallest anchor/);
  });

  it("throws on extrapolation above target distance", () => {
    assert.throws(() => getRatio(100, 150), /above target distance/);
  });
});
```

**Step 3: Implement**

```ts
import { RATIOS_BASE } from "./paceData";

export function getRatio(D: number, d: number): number {
  const anchors = RATIOS_BASE[D];
  if (!anchors) throw new Error(`Unsupported event distance: ${D}`);
  if (d < anchors[0].d) {
    throw new Error(`d=${d} below smallest anchor (${anchors[0].d}) for D=${D}`);
  }
  if (d > D) {
    throw new Error(`d=${d} above target distance D=${D}`);
  }
  // Exact match?
  for (const a of anchors) {
    if (a.d === d) return a.ratio;
  }
  // Find encadrement
  for (let i = 1; i < anchors.length; i++) {
    if (d < anchors[i].d) {
      const a1 = anchors[i - 1];
      const a2 = anchors[i];
      // R = R1 * (R2/R1)^((d-d1)/(d2-d1))
      const exp = (d - a1.d) / (a2.d - a1.d);
      return a1.ratio * Math.pow(a2.ratio / a1.ratio, exp);
    }
  }
  // Should be unreachable given the bounds checks above
  throw new Error(`Failed to interpolate d=${d} for D=${D}`);
}
```

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): getRatio with log-linear interpolation (doc §6)"
```

---

### Task 7: `strokeAdjustment()`

**Files:**
- Modify: `src/lib/paceCalculatorV2.ts`
- Modify: `src/__tests__/paceCalculatorV2.test.ts`

**Step 1: Tests**

```ts
import { strokeAdjustment } from "../lib/paceCalculatorV2";

describe("strokeAdjustment", () => {
  it("returns 1 at d = D (Tobj is conserved)", () => {
    assert.equal(strokeAdjustment(50, 50, "dos"), 1);
    assert.equal(strokeAdjustment(100, 100, "brasse"), 1);
    assert.equal(strokeAdjustment(400, 400, "papillon"), 1);
  });

  it("returns 1 for crawl at any distance (reference)", () => {
    assert.equal(strokeAdjustment(50, 25, "crawl"), 1);
    assert.equal(strokeAdjustment(200, 100, "crawl"), 1);
  });

  it("dos 50m at d=25 : 1 + 0.06 × (1 - 25/50)² = 1 + 0.06 × 0.25 = 1.015", () => {
    assert.equal(strokeAdjustment(50, 25, "dos"), 1.015);
  });

  it("brasse 100m at d=25 : 1 + 0.035 × (1 - 25/100)² = 1 + 0.035 × 0.5625 = 1.019687...", () => {
    const v = strokeAdjustment(100, 25, "brasse");
    assert.ok(Math.abs(v - 1.0196875) < 1e-6);
  });

  it("accepts override map", () => {
    const v = strokeAdjustment(50, 25, "dos", { dos: { "50m": 0.10 } });
    assert.equal(v, 1.025); // 1 + 0.10 × 0.25
  });
});
```

**Step 3: Implement**

```ts
import { STROKE_ADJUSTMENTS_DEFAULT } from "./paceData";

type SingleStroke = "crawl" | "dos" | "brasse" | "papillon";

export type StrokeAdjustmentOverrides = Partial<
  Record<SingleStroke, Partial<Record<EventFamily, number>>>
>;

export function strokeAdjustment(
  D: number,
  d: number,
  stroke: SingleStroke,
  overrides?: StrokeAdjustmentOverrides,
): number {
  if (d === D) return 1;
  const family = eventFamily(D);
  const m =
    overrides?.[stroke]?.[family] ??
    STROKE_ADJUSTMENTS_DEFAULT[stroke][family];
  return 1 + m * Math.pow(1 - d / D, 2);
}
```

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): strokeAdjustment A_nage(D,d,S) with overrides"
```

---

### Task 8: `computeTMax()` — t_MAX(d) for single strokes

**Files:**
- Modify: `src/lib/paceCalculatorV2.ts`
- Modify: `src/__tests__/paceCalculatorV2.test.ts`

**Step 1: Tests — reproducing doc §12.1 (50m crawl 23.62)**

```ts
import { computeTMax } from "../lib/paceCalculatorV2";

describe("computeTMax — doc §12.1 50m crawl 23.62", () => {
  const Tobj = 23.62;
  const D = 50;
  const stroke = "crawl";
  const cases: Array<[number, number]> = [
    [15, 5.69],
    [20, 8.17],
    [25, 10.65],
    [30, 13.25],
    [35, 15.85],
    [40, 18.42],
    [45, 21.02],
    [50, 23.62],
  ];
  for (const [d, expected] of cases) {
    it(`d=${d} → tMAX ≈ ${expected}`, () => {
      const got = computeTMax({ Tobj_s: Tobj, D, d, stroke });
      assert.ok(Math.abs(got - expected) < 0.01, `got ${got.toFixed(3)}, expected ${expected}`);
    });
  }
});

describe("computeTMax — doc §12.2 100m crawl 51.45", () => {
  const Tobj = 51.45;
  const D = 100;
  const stroke = "crawl";
  const cases: Array<[number, number]> = [
    [15, 5.87], [25, 11.01], [35, 16.26], [50, 24.18],
    [75, 37.82], [100, 51.45],
  ];
  for (const [d, expected] of cases) {
    it(`d=${d} → tMAX ≈ ${expected}`, () => {
      const got = computeTMax({ Tobj_s: Tobj, D, d, stroke });
      assert.ok(Math.abs(got - expected) < 0.01);
    });
  }
});

describe("computeTMax — doc §12.3 200m crawl 1:53.00 (113s)", () => {
  const Tobj = 113.00;
  const D = 200;
  const stroke = "crawl";
  const cases: Array<[number, number]> = [
    [25, 13.00], [50, 26.55], [75, 40.57], [100, 54.80],
    [150, 83.62], [200, 113.00],
  ];
  for (const [d, expected] of cases) {
    it(`d=${d} → tMAX ≈ ${expected}`, () => {
      const got = computeTMax({ Tobj_s: Tobj, D, d, stroke });
      assert.ok(Math.abs(got - expected) < 0.02);
    });
  }
});
```

**Step 3: Implement**

```ts
export function computeTMax(args: {
  Tobj_s: number;
  D: number;
  d: number;
  stroke: SingleStroke;
  delta_mesure_s?: number;
  adjustmentOverrides?: StrokeAdjustmentOverrides;
}): number {
  const { Tobj_s, D, d, stroke, delta_mesure_s = 0, adjustmentOverrides } = args;
  const R = getRatio(D, d);
  const A = strokeAdjustment(D, d, stroke, adjustmentOverrides);
  return Tobj_s * R * A + delta_mesure_s;
}
```

**Step 4: Run tests** — si certaines cellules attendues sont hors tolérance, les ratios `RATIOS_BASE` peuvent nécessiter un ajustement micro (le doc cite des arrondis). Si écart > 0.02s sur 1+ cellule, **stop et rapporte** : on ne tweake pas la table sans accord.

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): computeTMax + reproduction des exemples doc §12"
```

---

### Task 9: `computeZoneTime()` + `formatPaceTime` (réutilisé v1)

**Files:**
- Modify: `src/lib/paceCalculatorV2.ts`
- Modify: `src/__tests__/paceCalculatorV2.test.ts`

**Step 1: Tests**

```ts
import { computeZoneTime } from "../lib/paceCalculatorV2";
import { ZONE_COEFFICIENTS } from "../lib/paceData";

describe("computeZoneTime — doc §12.1", () => {
  it("V0 at d=25 in 50m crawl 23.62 ≈ 15.21", () => {
    const tMax = 10.65; // doc §12.1 25m row MAX
    const v0 = computeZoneTime({ tMax_s: tMax, zone: "V0", family: "50m", coefficientsOverride: undefined });
    assert.ok(Math.abs(v0 - 15.21) < 0.02);
  });

  it("V3 at d=50 in 100m crawl 51.45 ≈ 25.45", () => {
    const tMax = 24.18; // doc §12.2 50m row MAX
    const v3 = computeZoneTime({ tMax_s: tMax, zone: "V3", family: "100m" });
    assert.ok(Math.abs(v3 - 25.45) < 0.02);
  });

  it("throws if zone is V4 on a family where V4 is null (no override)", () => {
    assert.throws(
      () => computeZoneTime({ tMax_s: 100, zone: "V4", family: "400m" }),
      /V4 is not enabled/,
    );
  });

  it("accepts coefficient override (e.g., V4 enabled on 400m)", () => {
    const t = computeZoneTime({
      tMax_s: 100, zone: "V4", family: "400m",
      coefficientsOverride: { "400m": { V4: 0.97 } },
    });
    assert.equal(t, 100 / 0.97);
  });
});
```

**Step 3: Implement**

```ts
import { ZONE_COEFFICIENTS, type EventFamily, type Zone } from "./paceData";

export type ZoneCoefficientsOverride = Partial<
  Record<EventFamily, Partial<Record<Zone, number>>>
>;

export function computeZoneTime(args: {
  tMax_s: number;
  zone: Zone;
  family: EventFamily;
  coefficientsOverride?: ZoneCoefficientsOverride;
}): number {
  const { tMax_s, zone, family, coefficientsOverride } = args;
  const k =
    coefficientsOverride?.[family]?.[zone] ??
    ZONE_COEFFICIENTS[family][zone];
  if (k === null) {
    throw new Error(`V4 is not enabled for family ${family}. Override required.`);
  }
  return tMax_s / k;
}
```

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): computeZoneTime with override support for V4"
```

---

### Task 10: `compute4NSegment()` — segmented 4N

**Files:**
- Modify: `src/lib/paceCalculatorV2.ts`
- Modify: `src/__tests__/paceCalculatorV2.test.ts`

**Step 1: Tests — doc §9 example**

```ts
import { compute4NSegment, compute4NCumulative } from "../lib/paceCalculatorV2";

describe("compute4NSegment — doc §9", () => {
  it("200 4N, 25m brasse — Tobj=158.00s : T_brasse = 158 × 0.290 = 45.82, then R_50_brasse(25) = 0.451 × A_brasse(50,25) = 0.451 × (1 + 0.04 × 0.25) = 0.451 × 1.010 = 0.4555 → tMAX = 45.82 × 0.4555 = 20.87", () => {
    const got = compute4NSegment({
      Tobj_4N_s: 158.00,
      mode: "200",
      segment_stroke: "brasse",
      d_internal: 25,
    });
    assert.ok(Math.abs(got - 20.87) < 0.05, `got ${got.toFixed(3)}`);
  });

  it("400 4N, 50m papillon, Tobj=300s — T_pap = 300 × 0.229 = 68.70, R_100_papillon(50)=0.470, A_pap(100,50)=1+0×0.25=1.0 → tMAX = 68.70 × 0.470 = 32.29", () => {
    const got = compute4NSegment({
      Tobj_4N_s: 300.00,
      mode: "400",
      segment_stroke: "papillon",
      d_internal: 50,
    });
    assert.ok(Math.abs(got - 32.29) < 0.05);
  });
});

describe("compute4NCumulative — doc §9.3", () => {
  it("200 4N at d=125m : papillon (50) + dos (50) + brasse (25)", () => {
    const Tobj = 158.00;
    // T_pap = 158 × 0.218 = 34.444
    // T_dos = 158 × 0.250 = 39.50
    // brasse 25m segment = compute4NSegment(Tobj, 200, brasse, 25)
    const got = compute4NCumulative({ Tobj_4N_s: Tobj, mode: "200", d_cumulative: 125 });
    const brasse25 = compute4NSegment({ Tobj_4N_s: Tobj, mode: "200", segment_stroke: "brasse", d_internal: 25 });
    const expected = 34.444 + 39.50 + brasse25;
    assert.ok(Math.abs(got - expected) < 0.01);
  });
});
```

**Step 3: Implement**

```ts
import { SEGMENTS_4N } from "./paceData";

export function compute4NSegment(args: {
  Tobj_4N_s: number;
  mode: "200" | "400";
  segment_stroke: "papillon" | "dos" | "brasse" | "crawl";
  d_internal: number;     // 0 < d_internal ≤ segment_distance
  delta_mesure_s?: number;
  adjustmentOverrides?: StrokeAdjustmentOverrides;
}): number {
  const { Tobj_4N_s, mode, segment_stroke, d_internal, delta_mesure_s = 0, adjustmentOverrides } = args;
  const seg = SEGMENTS_4N[mode].find((s) => s.stroke === segment_stroke);
  if (!seg) throw new Error(`Stroke ${segment_stroke} not in 4N ${mode} segments`);
  if (d_internal <= 0 || d_internal > seg.segment_distance) {
    throw new Error(`d_internal=${d_internal} out of range (0, ${seg.segment_distance}]`);
  }
  const T_segment = Tobj_4N_s * seg.weight;
  const R = getRatio(seg.segment_distance, d_internal);
  const A = strokeAdjustment(seg.segment_distance, d_internal, segment_stroke, adjustmentOverrides);
  return T_segment * R * A + delta_mesure_s;
}

export function compute4NCumulative(args: {
  Tobj_4N_s: number;
  mode: "200" | "400";
  d_cumulative: number;
}): number {
  const { Tobj_4N_s, mode, d_cumulative } = args;
  const segs = SEGMENTS_4N[mode];
  const total_distance = segs.reduce((a, s) => a + s.segment_distance, 0);
  if (d_cumulative <= 0 || d_cumulative > total_distance) {
    throw new Error(`d_cumulative=${d_cumulative} out of (0, ${total_distance}]`);
  }
  let consumed = 0;
  let total_time = 0;
  for (const seg of segs) {
    if (d_cumulative >= consumed + seg.segment_distance) {
      total_time += Tobj_4N_s * seg.weight;
      consumed += seg.segment_distance;
    } else {
      const d_internal = d_cumulative - consumed;
      total_time += compute4NSegment({
        Tobj_4N_s, mode, segment_stroke: seg.stroke, d_internal,
      });
      break;
    }
  }
  return total_time;
}
```

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): 4N segmented calculation (compute4NSegment + cumulative)"
```

---

### Task 11: `getDistanceRowsV2()`

**Files:**
- Modify: `src/lib/paceCalculatorV2.ts`
- Modify: `src/__tests__/paceCalculatorV2.test.ts`

**Step 1: Tests** (per design doc §3.3)

```ts
import { getDistanceRowsV2 } from "../lib/paceCalculatorV2";

describe("getDistanceRowsV2", () => {
  it("D=50 single stroke → [15,20,25,30,35,40,45,50]", () => {
    assert.deepEqual(getDistanceRowsV2(50, "crawl"), [15, 20, 25, 30, 35, 40, 45, 50]);
    assert.deepEqual(getDistanceRowsV2(50, "papillon"), [15, 20, 25, 30, 35, 40, 45, 50]);
  });
  it("D=100 → [15,25,35,50,65,75,100]", () => {
    assert.deepEqual(getDistanceRowsV2(100, "dos"), [15, 25, 35, 50, 65, 75, 100]);
  });
  it("D=200 → [25,50,75,100,150,200]", () => {
    assert.deepEqual(getDistanceRowsV2(200, "brasse"), [25, 50, 75, 100, 150, 200]);
  });
  it("D=400 → [50,75,100,150,200,300,400]", () => {
    assert.deepEqual(getDistanceRowsV2(400, "crawl"), [50, 75, 100, 150, 200, 300, 400]);
  });
  it("D=800 → [50,100,200,300,400,600,800]", () => {
    assert.deepEqual(getDistanceRowsV2(800, "crawl"), [50, 100, 200, 300, 400, 600, 800]);
  });
  it("D=1500 → [50,100,200,300,400,800,1000,1500]", () => {
    assert.deepEqual(getDistanceRowsV2(1500, "crawl"), [50, 100, 200, 300, 400, 800, 1000, 1500]);
  });
  it("4N : returns empty (4N uses segmented matrix, not row list)", () => {
    assert.deepEqual(getDistanceRowsV2(200, "4N"), []);
  });
});
```

**Step 3: Implement**

```ts
const ROWS_BY_D: Record<number, number[]> = {
  50:   [15, 20, 25, 30, 35, 40, 45, 50],
  100:  [15, 25, 35, 50, 65, 75, 100],
  200:  [25, 50, 75, 100, 150, 200],
  400:  [50, 75, 100, 150, 200, 300, 400],
  800:  [50, 100, 200, 300, 400, 600, 800],
  1500: [50, 100, 200, 300, 400, 800, 1000, 1500],
};

export function getDistanceRowsV2(D: number, stroke: StrokeV2): number[] {
  if (stroke === "4N") return [];
  return ROWS_BY_D[D] ?? [];
}
```

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): getDistanceRowsV2 — distances étendues du doc §3.3"
```

---

### Task 12: `validateMatrix()` — coherence checks (doc §11)

**Files:**
- Modify: `src/lib/paceCalculatorV2.ts`
- Modify: `src/__tests__/paceCalculatorV2.test.ts`

**Step 1: Tests**

```ts
import { validateMatrix } from "../lib/paceCalculatorV2";

describe("validateMatrix", () => {
  it("returns [] for a coherent matrix", () => {
    const m = {
      Tobj_s: 23.62, D: 50,
      rows: [
        { d: 25, tMax_s: 10.65, zones: { V0: 15.21, V1: 13.66, V2: 12.39, V3: 11.33, V4: 10.87, MAX: 10.65 } },
        { d: 50, tMax_s: 23.62, zones: { V0: 33.74, V1: 30.28, V2: 27.47, V3: 25.13, V4: 24.10, MAX: 23.62 } },
      ],
    };
    assert.deepEqual(validateMatrix(m), []);
  });
  it("flags non-monotonic tMax", () => {
    const m = {
      Tobj_s: 23.62, D: 50,
      rows: [
        { d: 25, tMax_s: 11.00, zones: {} as any },
        { d: 50, tMax_s: 10.00, zones: {} as any }, // tMax decreases → flag
      ],
    };
    const errs = validateMatrix(m);
    assert.ok(errs.some((e) => e.includes("monotonie")));
  });
  it("flags zones not properly ordered", () => {
    const m = {
      Tobj_s: 23.62, D: 50,
      rows: [
        { d: 25, tMax_s: 10.65, zones: { V0: 12.0, V1: 13.0, V2: 11.0, V3: 11.33, V4: 10.87, MAX: 10.65 } as any },
      ],
    };
    const errs = validateMatrix(m);
    assert.ok(errs.some((e) => e.includes("ordre")));
  });
  it("flags tMax(D) ≠ Tobj", () => {
    const m = {
      Tobj_s: 50.0, D: 50,
      rows: [{ d: 50, tMax_s: 49.5, zones: {} as any }],
    };
    const errs = validateMatrix(m);
    assert.ok(errs.some((e) => e.includes("Tobj")));
  });
});
```

**Step 3: Implement**

```ts
export interface MatrixForValidation {
  Tobj_s: number;
  D: number;
  rows: Array<{
    d: number;
    tMax_s: number;
    zones: Partial<Record<Zone, number>>;
  }>;
}

export function validateMatrix(m: MatrixForValidation): string[] {
  const errors: string[] = [];
  const sorted = [...m.rows].sort((a, b) => a.d - b.d);

  // Check 1: monotonie tMax
  for (let i = 1; i < sorted.length; i++) {
    if (sorted[i].tMax_s <= sorted[i - 1].tMax_s) {
      errors.push(`monotonie violée : tMAX(${sorted[i].d}) <= tMAX(${sorted[i - 1].d})`);
    }
  }

  // Check 2: tMax(D) ≈ Tobj
  const last = sorted[sorted.length - 1];
  if (last && last.d === m.D) {
    if (Math.abs(last.tMax_s - m.Tobj_s) > 0.05) {
      errors.push(`tMax(${m.D}) = ${last.tMax_s} ne correspond pas à Tobj=${m.Tobj_s}`);
    }
  }

  // Check 3: zones strictly ordered V0 > V1 > V2 > V3 > V4? > MAX (en TIME)
  const zoneOrder: Zone[] = ["V0", "V1", "V2", "V3", "V4", "MAX"];
  for (const row of sorted) {
    const present = zoneOrder.filter((z) => row.zones[z] !== undefined);
    for (let i = 1; i < present.length; i++) {
      const prev = row.zones[present[i - 1]]!;
      const cur = row.zones[present[i]]!;
      if (prev <= cur) {
        errors.push(`ordre des zones violé à d=${row.d} : ${present[i - 1]}=${prev} <= ${present[i]}=${cur}`);
      }
    }
  }

  return errors;
}
```

**Step 5: Commit**
```bash
git commit -m "feat(pace-v2): validateMatrix — 3 checks doc §11"
```

---

## Phase 3 — Migration

### Task 13: Migration 00151 — schema only

**Files:**
- Create: `supabase/migrations/00151_pace_model_v2.sql`

Contenu (copier verbatim depuis design doc §5.1, ajouter sections (b)(c)(d) restantes — voir design pour le détail) :

```sql
-- (a) Refonte coach_pace_zones (DROP + recréation, pas de fallback v1)
DROP TABLE IF EXISTS coach_pace_zones CASCADE;

CREATE TABLE coach_pace_zones (
  coach_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  event_family text NOT NULL CHECK (event_family IN ('50m','100m','200m','400m','800m_1500m')),
  zone text NOT NULL CHECK (zone IN ('V0','V1','V2','V3','V4','MAX')),
  k_value numeric(5,4) NOT NULL CHECK (k_value > 0 AND k_value <= 1),
  updated_at timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (coach_id, event_family, zone)
);
ALTER TABLE coach_pace_zones ENABLE ROW LEVEL SECURITY;
CREATE POLICY "coach_pace_zones_select_own" ON coach_pace_zones FOR SELECT USING (coach_id = (SELECT auth.uid()));
CREATE POLICY "coach_pace_zones_insert_own" ON coach_pace_zones FOR INSERT WITH CHECK (coach_id = (SELECT auth.uid()));
CREATE POLICY "coach_pace_zones_update_own" ON coach_pace_zones FOR UPDATE USING (coach_id = (SELECT auth.uid())) WITH CHECK (coach_id = (SELECT auth.uid()));
CREATE POLICY "coach_pace_zones_delete_own" ON coach_pace_zones FOR DELETE USING (coach_id = (SELECT auth.uid()));

-- (b) coach_stroke_adjustments (override coach des mS)
CREATE TABLE coach_stroke_adjustments (
  coach_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  stroke text NOT NULL CHECK (stroke IN ('crawl','dos','brasse','papillon')),
  event_family text NOT NULL CHECK (event_family IN ('50m','100m','200m','400m','800m_1500m')),
  m_value numeric(5,4) NOT NULL CHECK (m_value >= -0.20 AND m_value <= 0.20),
  updated_at timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (coach_id, stroke, event_family)
);
ALTER TABLE coach_stroke_adjustments ENABLE ROW LEVEL SECURITY;
CREATE POLICY "coach_stroke_adj_select_own" ON coach_stroke_adjustments FOR SELECT USING (coach_id = (SELECT auth.uid()));
CREATE POLICY "coach_stroke_adj_insert_own" ON coach_stroke_adjustments FOR INSERT WITH CHECK (coach_id = (SELECT auth.uid()));
CREATE POLICY "coach_stroke_adj_update_own" ON coach_stroke_adjustments FOR UPDATE USING (coach_id = (SELECT auth.uid())) WITH CHECK (coach_id = (SELECT auth.uid()));
CREATE POLICY "coach_stroke_adj_delete_own" ON coach_stroke_adjustments FOR DELETE USING (coach_id = (SELECT auth.uid()));

-- (c) Préserver upsert_pace_target / get_pace_share_payload (RPCs §185).
-- Ces fonctions consomment coach_pace_zones — on droppe et recrée pour qu'elles compilent
-- avec le nouveau schema. La logique métier inchangée :
-- - upsert_pace_target ne lit pas zones (ok, no-op)
-- - get_pace_share_payload sélectionne row_to_json(z) FROM coach_pace_zones z WHERE coach_id = ...
--   → lecture multi-row désormais ; doit retourner un agrégat jsonb. Voir migration suivante (Task 17).
```

**Step 1: Apply via MCP**

```
mcp__plugin_supabase_supabase__apply_migration
  project_id: fscnobivsgornxdwqwlk
  name: "00151_pace_model_v2"
  query: <contenu du .sql>
```

**Step 2: Verify**

```
mcp__plugin_supabase_supabase__execute_sql
  query: |
    SELECT column_name FROM information_schema.columns
    WHERE table_schema='public' AND table_name='coach_pace_zones'
    ORDER BY ordinal_position;
    SELECT count(*) FROM coach_pace_zones; -- doit être 0
    SELECT count(*) FROM coach_stroke_adjustments; -- doit être 0
```

Attendu : colonnes `coach_id, event_family, zone, k_value, updated_at`.

**Step 3: Mirror dans test schema**

Dupliquer les CREATE/POLICY dans `supabase/tests/schema.sql`.

**Step 4: Commit**
```bash
git add supabase/migrations/00151_pace_model_v2.sql supabase/tests/schema.sql
git commit -m "feat(db): 00151 — refonte coach_pace_zones v2 + coach_stroke_adjustments"
```

---

### Task 14: RPC `get_pace_share_payload` — adapter au nouveau schema

**Files:**
- Modify: `supabase/migrations/00151_pace_model_v2.sql` (append) **ou** créer `00152_pace_share_payload_v2.sql` si 00151 déjà appliqué.

Le RPC §185 lit `coach_pace_zones` comme une seule row de %. En v2, c'est multi-row. On doit retourner un objet structuré :

```sql
CREATE OR REPLACE FUNCTION get_pace_share_payload(token_in uuid)
RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
DECLARE
  link record;
  swimmer_name text;
  swimmer_sex text;
  zones_v2 jsonb;
  targets jsonb;
BEGIN
  SELECT * INTO link FROM pace_share_links WHERE token = token_in AND expires_at > now();
  IF NOT FOUND THEN RETURN NULL; END IF;

  IF link.swimmer_account_id IS NOT NULL THEN
    SELECT u.display_name, p.sex INTO swimmer_name, swimmer_sex
      FROM users u LEFT JOIN user_profiles p ON p.user_id = u.id
      WHERE u.id = link.swimmer_account_id;
  ELSE
    SELECT display_name, sex INTO swimmer_name, swimmer_sex
      FROM coach_manual_swimmers WHERE id = link.swimmer_manual_id;
  END IF;

  SELECT jsonb_object_agg(event_family, family_zones) INTO zones_v2
    FROM (
      SELECT event_family, jsonb_object_agg(zone, k_value) AS family_zones
        FROM coach_pace_zones WHERE coach_id = link.coach_id
        GROUP BY event_family
    ) t;

  SELECT jsonb_agg(t) INTO targets FROM coach_pace_targets t
    WHERE coach_id = link.coach_id
      AND ((swimmer_account_id IS NOT NULL AND swimmer_account_id = link.swimmer_account_id)
        OR (swimmer_manual_id IS NOT NULL AND swimmer_manual_id = link.swimmer_manual_id));

  RETURN jsonb_build_object(
    'swimmer_name', swimmer_name,
    'swimmer_sex', swimmer_sex,
    'zones_v2', COALESCE(zones_v2, '{}'::jsonb),
    'targets', COALESCE(targets, '[]'::jsonb)
  );
END;
$$;
GRANT EXECUTE ON FUNCTION get_pace_share_payload(uuid) TO anon, authenticated;
```

**Step : Apply via MCP** (même protocole que Task 13).

**Commit** : `feat(db): 00151 — RPC get_pace_share_payload adapté schema v2`

---

## Phase 4 — API modules

### Task 15: `pace-zones.ts` — refonte

**Files:**
- Modify: `src/lib/api/pace-zones.ts`
- Modify: `src/lib/api/__tests__/pace-zones.test.ts`

Ancienne API (5 colonnes pct) **retirée**. Nouvelle API :

```ts
import { ZONE_COEFFICIENTS, type EventFamily, type Zone } from "@/lib/paceData";
import { supabase, canUseSupabase } from "./client";

export interface ZoneRow {
  coach_id: string;
  event_family: EventFamily;
  zone: Zone;
  k_value: number;
  updated_at: string;
}

/** Returns map family→zone→k. Initializes defaults if no row exists. */
export async function getMyPaceZonesV2(): Promise<Record<EventFamily, Partial<Record<Zone, number>>>>;

/** Upserts a single (family, zone, k). */
export async function upsertPaceZoneCell(args: {
  event_family: EventFamily;
  zone: Zone;
  k_value: number;
}): Promise<void>;

/** Resets all zones for current coach to doc defaults. */
export async function resetMyPaceZonesToDefaults(): Promise<void>;

/** Initializes 25 default rows (5 families × 5 mandatory zones, V4 omitted by default). */
export async function initMyPaceZonesIfMissing(): Promise<boolean>;
```

**Tests** (4-5 cas, pattern node:test + mock.module — calque assignments.test.ts) :
- `getMyPaceZonesV2` retourne `ZONE_COEFFICIENTS` quand aucune row (init implicite via `initMyPaceZonesIfMissing` côté hook).
- `upsertPaceZoneCell` envoie `(coach_id, family, zone, k)` avec `onConflict: "coach_id,event_family,zone"`.
- `resetMyPaceZonesToDefaults` delete + bulk insert des 25 défauts.

**Commit** : `refactor(api): pace-zones — refonte schema v2 (family × zone)`

---

### Task 16: `pace-stroke-adjustments.ts` — nouveau module

**Files:**
- Create: `src/lib/api/pace-stroke-adjustments.ts`
- Test: sibling (node:test)

API :

```ts
export interface StrokeAdjustmentRow {
  coach_id: string;
  stroke: SingleStroke;
  event_family: EventFamily;
  m_value: number;
}
export async function getMyStrokeAdjustments(): Promise<StrokeAdjustmentRow[]>;
export async function upsertStrokeAdjustment(args: {
  stroke: SingleStroke;
  event_family: EventFamily;
  m_value: number;
}): Promise<void>;
export async function resetMyStrokeAdjustments(): Promise<void>;
```

Implémentation analogue à `pace-zones`. Tests minimaux (3 cas).

**Commit** : `feat(api): pace-stroke-adjustments — overrides coach des mS`

---

### Task 17: Façade `api.ts` + re-exports

**Files:**
- Modify: `src/lib/api/index.ts`
- Modify: `src/lib/api.ts`

Re-exporter les nouvelles fonctions, retirer celles de l'ancien `pace-zones` (signature changée : `getMyPaceZones` retourne désormais `Record<EventFamily, ...>`, pas une struct plate). Mettre à jour les call-sites.

**Run** : `npx tsc --noEmit` doit passer.

**Commit** : `chore(api): façade — re-exports v2 pace`

---

## Phase 5 — Hook + initialisation des défauts

### Task 18: `useCoachPaceZonesV2()` hook

**Files:**
- Create: `src/hooks/useCoachPaceZonesV2.ts`
- Test: sibling

Comportement :
- React Query : `["coach-pace-zones-v2"]`, `staleTime: Infinity`
- `queryFn` : `getMyPaceZonesV2()`. Si vide → `initMyPaceZonesIfMissing()` (insère 25 défauts), puis re-query.
- Expose : `{ zones, isLoading, error, upsertCell, resetToDefaults, toggleV4 }`

**Test** :
- Mount → init triggered une fois si DB vide
- `upsertCell` invalide la query
- `resetToDefaults` → 25 rows seed

**Commit** : `feat(hooks): useCoachPaceZonesV2 — init idempotente + helpers`

---

### Task 19: `useCoachStrokeAdjustments()` hook

**Files:**
- Create: `src/hooks/useCoachStrokeAdjustments.ts`
- Test: sibling

Pas d'init automatique : si pas d'override, on lit directement `STROKE_ADJUSTMENTS_DEFAULT` (constante client). Le hook retourne le merge `defaults ∪ overrides_coach`.

API : `{ adjustments, upsertOne, resetToDefaults }`.

**Commit** : `feat(hooks): useCoachStrokeAdjustments — merge défauts + overrides`

---

## Phase 6 — Refonte UI

### Task 20: Refonte `PaceMatrix.tsx`

**Files:**
- Modify: `src/components/coach/pace/PaceMatrix.tsx`
- Modify: `src/components/coach/pace/__tests__/PaceMatrix.test.tsx`

**INVOCATION `/frontend-design` OBLIGATOIRE.**

Props nouvelles :
```ts
interface Props {
  targetTimeMs: number;
  targetDistanceM: number;     // 50, 100, 200, 400, 800, 1500
  stroke: StrokeV2;
  swimmerSex: "M" | "F" | null;  // pour pool conversion §185
  targetPool: PoolSize;
  zones: Record<EventFamily, Partial<Record<Zone, number>>>;
  strokeAdjustments: StrokeAdjustmentOverrides;
  v4EnabledForFamily: boolean;  // toggle V4 sur 200/400/800/1500
}
```

Rendu :
- Détermine `family = eventFamily(targetDistanceM)`
- Lignes = `getDistanceRowsV2(targetDistanceM, stroke)`
- Colonnes = `[V0, V1, V2, V3]` + (V4 si présent dans family.zones et v4EnabledForFamily) + `[MAX]`
- Pour chaque (d, zone) : `tMax = computeTMax({Tobj, D, d, stroke, adjustmentOverrides})`, puis `t_zone = computeZoneTime({tMax, zone, family, coefficientsOverride: zones})`
- Pool conversion §185 appliquée en post-calcul (réutilise `convertTargetTime` côté affichage si `viewPool ≠ targetPool`)

Cellules zones spéciales :
- Cellule MAX en gras / bordure marquée
- V4 en couleur intermédiaire entre V3 et MAX
- Ligne `d = D` mise en évidence (background subtle)

Footer : "Modèle non-linéaire v2 (basé sur regles_calcul_allures_natation.docx). Coefficients à calibrer par tests individuels — voir §187."

**Tests** (node:test + Testing Library) :
- Render `Tobj=23.62, D=50, stroke=crawl` → cellule (25, V0) ≈ "15.2"
- V4 invisible quand `v4EnabledForFamily=false` ET `family ∈ {200m, 400m, 800m_1500m}`
- V4 visible quand `family ∈ {50m, 100m}` (toujours) ou `v4EnabledForFamily=true`
- Stroke `4N` → render placeholder "Voir matrice 4N segmentée"
- Pool conversion appliquée (cas test : viewPool=25m → cellules recalculées)

**Commit** : `feat(pace-v2): refonte PaceMatrix — modèle non-linéaire + V4 conditionnel`

---

### Task 21: Nouveau composant `Pace4NSegmentMatrix.tsx`

**Files:**
- Create: `src/components/coach/pace/Pace4NSegmentMatrix.tsx`
- Test: sibling

**INVOCATION `/frontend-design` OBLIGATOIRE.**

Props :
```ts
interface Props {
  targetTimeMs: number;
  mode: "200" | "400";
  swimmerSex: "M" | "F" | null;
  targetPool: PoolSize;
  zones: ...;
  strokeAdjustments: ...;
}
```

Layout : 4 sous-matrices empilées, une par segment (papillon → dos → brasse → crawl). Chaque sous-matrice :
- En-tête : "Papillon · 50m (segment) · t_segment = X.XX"
- Lignes (200 4N) : 25, 50 (segment complet)
- Lignes (400 4N) : 50, 100 (segment complet)
- Colonnes : V0..MAX de la famille du segment (50m pour 200 4N ; 100m pour 400 4N)

Récap "cumulés" en bas :
- Pour 200 4N : 100 (= papillon + dos), 150 (+brasse), 200 (+crawl) — colonne unique tMAX
- Pour 400 4N : 200, 300, 400 — idem

Tests : exemples doc §9 reproduits.

**Commit** : `feat(pace-v2): Pace4NSegmentMatrix — segmenté avec poids`

---

### Task 22: `PaceZonesSettings.tsx` — refonte avec tabs par famille

**Files:**
- Modify: `src/components/coach/pace/PaceZonesSettings.tsx`
- Modify: `src/components/coach/pace/__tests__/PaceZonesSettings.test.tsx`

**INVOCATION `/frontend-design` OBLIGATOIRE.**

Layout :
- Sheet right/bottom (existant)
- `<Tabs>` avec 5 tabs : `50m | 100m | 200m | 400m | 800-1500m`
- Pour chaque tab : 5 sliders (V0, V1, V2, V3, MAX) + 1 toggle "Activer V4" (si famille ∈ {200m, 400m, 800m_1500m}, le toggle apparaît ; sinon V4 toujours présent pour 50m/100m)
- Validation : V0 < V1 < V2 < V3 < (V4) < MAX en SPEED → save disabled si l'ordre est cassé
- Bouton "Réinitialiser cette famille" (par tab) + "Réinitialiser tout" (global)
- Save → `upsertPaceZoneCell` × N cells modifiées

**Tests** :
- Render avec defaults → 5 tabs, V4 visible 50m/100m
- Toggle V4 sur 400m → V4 input apparaît
- Drag V2 < V3 → save disabled
- Reset famille → cells de cette famille reviennent au défaut

**Commit** : `feat(pace-v2): PaceZonesSettings — tabs par famille + V4 togglable`

---

### Task 23: Nouveau drawer `PaceStrokeAdjustments.tsx`

**Files:**
- Create: `src/components/coach/pace/PaceStrokeAdjustments.tsx`
- Test: sibling

**INVOCATION `/frontend-design` OBLIGATOIRE.**

Layout :
- Sheet right/bottom
- Grille 4 nages × 5 familles, chaque cellule un input `numeric` (range -0.20 à 0.20, step 0.005)
- En-tête : explication courte "mS module l'ajustement par nage (1 - d/D)². Valeur 0 = même courbe que le crawl."
- Bouton "Réinitialiser aux médianes du doc"
- Save → `upsertStrokeAdjustment` × N

**Tests** : 4 cas de base.

**Commit** : `feat(pace-v2): PaceStrokeAdjustments — drawer mS overrides`

---

### Task 24: `SwimmerPaceCard.tsx` — adapter aux nouvelles props

**Files:**
- Modify: `src/components/coach/pace/SwimmerPaceCard.tsx`

Récupère `zones`, `strokeAdjustments`, `v4EnabledForFamily` du parent, les forward à `PaceMatrix` ou `Pace4NSegmentMatrix` selon `target.stroke`.

Pas de tests ajoutés (les tests existants doivent rester verts ou être adaptés).

**Commit** : `refactor(pace-v2): SwimmerPaceCard — propage zones + strokeAdjustments`

---

### Task 25: `CoachPaceCalculatorScreen.tsx` — refonte

**Files:**
- Modify: `src/pages/coach/CoachPaceCalculatorScreen.tsx`
- Modify: `src/pages/coach/__tests__/CoachPaceCalculatorScreen.test.tsx`

**INVOCATION `/frontend-design` OBLIGATOIRE.**

Composition :
- `useCoachPaceZonesV2` (init défauts + upsertCell)
- `useCoachStrokeAdjustments` (merge defaults + overrides)
- `useQuery(["pace-targets"], listMyPaceTargets)` (inchangé §184)
- État local : `expandedSwimmerIds`, `zonesSettingsOpen`, `adjustmentsOpen`, `v4ByFamily: Record<EventFamily, boolean>` (init from doc defaults)

Toolbar :
- Bouton "Zones" → ouvre `PaceZonesSettings`
- Bouton "Ajustements nages" → ouvre `PaceStrokeAdjustments`
- Bouton "V4" (drop-down ou popover) avec checkbox par famille pour l'activer

Pour chaque card swimmer : passer les props préparées à `SwimmerPaceCard`.

Tests : smoke render, ouverture des 3 drawers, upsert d'une zone propagé.

**Commit** : `refactor(pace-v2): CoachPaceCalculatorScreen — wire zones + adjustments + V4`

---

### Task 26: `PaceTargetForm.tsx` — refus NAC/Spé + label

**Files:**
- Modify: `src/components/coach/pace/PaceTargetForm.tsx`

Refuser les nages hors {NL, Dos, Brasse, Pap, 4N} avec un toast "Nage non gérée par le modèle d'allures". (Si la liste actuelle est déjà restreinte, no-op.)

Distance options : pas de changement structurel mais filtrage selon nage (4N → {100, 200, 400} ; sinon {50, 100, 200, 400, 800, 1500} pour NL ; pour Dos/Brasse/Pap → {50, 100, 200} car ce sont les seules épreuves officielles).

**Tests** : 1 cas par filtre.

**Commit** : `feat(pace-v2): PaceTargetForm — restreindre les distances par nage selon FFN`

---

## Phase 7 — Export PDF + Page partagée

### Task 27: `export-pace-pdf.ts` — adapter au modèle v2

**Files:**
- Modify: `src/lib/export-pace-pdf.ts`
- Modify: `src/__tests__/export-pace-pdf.test.ts`

Recalculer les matrices avec `paceCalculatorV2`. Header de chaque target garde le bassin + temps cible + zones perso (mention "selon zones coach v2").

Pour 4N : layout dédié (4 sous-tableaux empilés).

**Tests** : 1 cas (cible 50 NL) + 1 cas 4N → blob valide.

**Commit** : `feat(pace-v2): export-pace-pdf — adapter au modèle v2`

---

### Task 28: `SharedPaceMatrix.tsx` — adapter au payload v2

**Files:**
- Modify: `src/pages/SharedPaceMatrix.tsx`
- Modify: tests siblings

Le RPC `get_pace_share_payload` retourne désormais `zones_v2` (Record<EventFamily, Record<Zone, number>>). Adapter le composant pour reconstruire le rendu avec `paceCalculatorV2`.

**Commit** : `feat(pace-v2): SharedPaceMatrix — consume zones_v2 payload`

---

## Phase 8 — RLS tests

### Task 29: RLS test `coach_pace_zones` v2

**Files:**
- Modify: `supabase/tests/rls/coach_pace_zones.test.ts`

Adapter les tests existants au nouveau schema (multi-row par coach). Cas :
1. Insert (50m, V0, 0.70) coach A → OK.
2. Insert même row coach B avec coach_id A → erreur RLS WITH CHECK.
3. Coach B SELECT row de A → 0 ligne.
4. CHECK k_value > 0 et ≤ 1 (insert k=0 → erreur ; k=1.5 → erreur).
5. CHECK event_family ∈ enum (insert "300m" → erreur).
6. CHECK zone ∈ enum (insert "V5" → erreur).
7. UPDATE/DELETE isolation cross-coach.

Mettre à jour `supabase/tests/schema.sql` cohérent avec migration 00151 (Task 13 déjà fait).

**Commit** : `test(rls): coach_pace_zones v2 — schema multi-row + CHECKs`

---

### Task 30: RLS test `coach_stroke_adjustments`

**Files:**
- Create: `supabase/tests/rls/coach_stroke_adjustments.test.ts`

Cas :
1. Insert (crawl, 100m, 0.05) coach A → OK.
2. Coach B SELECT/UPDATE/DELETE row de A → 0 lignes affectées / rejet RLS.
3. CHECK m_value ∈ [-0.20, 0.20] (insert 0.5 → erreur).
4. CHECK stroke ∈ {crawl, dos, brasse, papillon} (insert "NAC" → erreur).

**Commit** : `test(rls): coach_stroke_adjustments — coverage initiale`

---

### Task 31: Run RLS suite + non-régression

**Pré-flight** :
1. `docker ps` (1× max). Si Docker down → demande user, n'essaie pas de lancer.
2. Si supabase pas up : `supabase start`.

**Run** :
```bash
npm run test:rls
```

Attendu : 175 (existant) − tests modifiés `coach_pace_zones` + tests modifiés/nouveaux = baseline cohérent. **Aucune régression** sur les tests d'autres tables.

Si fail → debug via `docs/rls-testing.md` (Token-coût élevé : justifier).

**Commit** : `test(rls): suite verte avec schema v2 (175+N pass)`

---

## Phase 9 — Documentation & PR

### Task 32: `docs/implementation-log.md` — entrée §186

Décrire : refonte modèle, fichiers touchés (+wc -l pour ≥150 LOC), tests ajoutés, décisions, limites.

### Task 33: `docs/ROADMAP.md` — ligne §186 + bumper "Dernière mise à jour"

### Task 34: `docs/FEATURES_STATUS.md` — toggle "Calculateur d'allures (modèle non-linéaire)" → ✅

### Task 35: `CLAUDE.md` — bumper "Dernière entrée en date : §186 (...description...)" + ajouter `paceCalculatorV2.ts` + `paceData.ts` au bloc Hubs si applicable.

### Task 36: `docs/claude/files-map.md` — ajouter les nouveaux fichiers ≥150 LOC :
- `src/lib/paceCalculatorV2.ts`
- `src/lib/paceData.ts`
- `src/components/coach/pace/Pace4NSegmentMatrix.tsx`
- `src/components/coach/pace/PaceStrokeAdjustments.tsx`
- (autres si pertinents — mesurer via `wc -l`)

### Task 37: Vérifications finales + PR

```bash
npx tsc --noEmit
npm test -- --run         # X pass / 1 fail (transformers)
npm run test:rls          # déjà fait Task 31
git status                # clean
git log --oneline main..HEAD | head -40  # liste les commits du chantier
```

Push :
```bash
git push -u origin chantier/186-pace-model-v2
gh pr create --base main --title "feat(pace): §186 — refonte modèle non-linéaire" --body "..."
```

---

## Risques & gotchas (rappel)

- **Migration via MCP uniquement** — jamais `supabase db push`.
- **Tests = node:test, PAS Vitest** — calque `assignments.test.ts`, sinon erreur §184 task fix.
- **Exemples doc §12** sont la vérité. Si une cellule diverge > 0.02s, **stop et rapporte** avant tweak.
- **Pool conversion §185** doit rester intacte côté affichage. Le moteur v2 calcule en référence 50m, FFN appliquée en post-render.
- **NAC, Spé** refusés à la création de cible (UX explicite). Pas de fallback v1 (modèle v1 supprimé).
- **PDF + Share** : adapter sans casser le contrat utilisateur (les PDFs déjà téléchargés restent lisibles, les liens partagés non expirés doivent toujours répondre).

## Hors scope §186 (acté)

- Calibration individuelle par tests réels → §187
- Δ_mesure (départ plongé/poussé/virages) → §187
- Audit trail des paramètres → §188
- Exposition API "ajout d'une distance ad-hoc dans la matrice" via input "+" → reportée à §187 si scope §186 explose ; sinon incluse en sous-tâche optionnelle de §186.
